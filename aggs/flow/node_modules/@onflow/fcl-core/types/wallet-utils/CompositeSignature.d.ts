/**
 * @description
 * Constructs a new CompositeSignature instance.
 *
 * @param {string} addr - Flow Address
 * @param {number} keyId - Key ID
 * @param {string} signature - Signature as a hex string
 */
export function CompositeSignature(addr: string, keyId: number, signature: string): void;
export class CompositeSignature {
    /**
     * @description
     * Constructs a new CompositeSignature instance.
     *
     * @param {string} addr - Flow Address
     * @param {number} keyId - Key ID
     * @param {string} signature - Signature as a hex string
     */
    constructor(addr: string, keyId: number, signature: string);
    f_type: string;
    f_vsn: string;
    addr: string;
    keyId: number;
    signature: string;
}
