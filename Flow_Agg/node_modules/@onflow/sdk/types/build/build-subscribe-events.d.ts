import { EventFilter } from "@onflow/typedefs";
/**
 * Subscribe to events with the given filter & parameters
 */
export declare function subscribeEvents({ startBlockId, startHeight, eventTypes, addresses, contracts, heartbeatInterval, }: EventFilter & {
    startBlockId?: string;
    startHeight?: number;
    heartbeatInterval?: number;
}): Function;
